# ==============================================
# SimForge Python Dependencies
# ==============================================

# Core parsing / XML
lxml
pyyaml

# Network extraction from OSM
# pyosmium (package name: "osmium") slices hash-pinned Geofabrik PBFs in
# osm_data/ by bbox; osmnx parses the slice into a MultiDiGraph. The live
# Overpass path via osmnx is retained only for bboxes with no local PBF.
osmnx>=2.0,<3
osmium>=4.0
networkx
geopandas>=1.0,<2
shapely>=1.8,<3

# Numerical / data
numpy
pandas

# Plotting (evaluation & thesis figures)
matplotlib>=3.5
seaborn>=0.12

# Visualization component (visualization/) — choropleth renderer reads
# US Census Bureau Cartographic Boundary tract shapefiles via pyshp.
# Pure-Python ESRI shapefile reader; no native deps. Used only by
# `python -m visualization.generate_maps --style choropleth`.
pyshp>=2.3,<4

# Testing
pytest

# DTALite engine (CPU mesoscopic Dynamic Traffic Assignment).
# Bundled binary ships inside the path4gmns package — no separate install.
# On macOS the bundled binary needs the OpenMP runtime: `brew install libomp`.
path4gmns>=0.10.0,<1

# ==============================================
# EXTERNAL DEPENDENCIES (NOT PIP PACKAGES)
# ==============================================
# These are installed separately - NOT via pip:
#
# SUMO (Simulation of Urban MObility)
#   - macOS: brew install sumo
#   - Linux: apt install sumo sumo-tools
#   - Download: https://sumo.dlr.de/docs/Downloads.php
#
# MATSim (Multi-Agent Transport Simulation)
#   - Java JAR file, not a pip package
#   - Download: https://github.com/matsim-org/matsim-libs/releases
#   - Install to: lib/matsim-15.0/matsim-15.0.jar
#   - Requires: Java 17+ (brew install openjdk@17)
#
# DTALite system dep on macOS only:
#   brew install libomp     # OpenMP runtime for the bundled .dylib
# ==============================================