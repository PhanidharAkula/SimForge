"""
SimForge preset configurations.

Each preset is a ready-to-use scenario configuration with pre-defined
parameters.  Users can run these directly for quick testing or use them
as templates for custom configurations.

Usage:
    python -m presets.run quick_test
    python -m presets.run morning_rush
    python -m presets.run --list
"""
